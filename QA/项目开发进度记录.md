# 项目开发进度记录

更新时间：2026-05-08

## 已完成
- Agent 注册、心跳、在线状态和调用图谱口径已统一到 `ai_agent + ai_agent_heartbeat`。
- 模型管理已支持 MiniMax / OpenAI Compatible 配置，页面支持中英文切换。
- 自动化流水线已覆盖 PRD 自动生成、审核、拒绝后重生成、代码生成队列、代码模板管理和生成文件树预览。
- Agent 质量监控已支持模型、数据集、topK、temperature 配置，并展示 accuracy、precision、recall、F1。
- 低代码调用记录表已补齐，调用历史和下载链路可用。

## 本次新增
- 新增 RAG 知识库菜单页，可选择已配置的 bgem3 / embedding 模型。
- 支持配置 Chroma 地址、集合名称、分块大小、重叠长度，并把文档拆分后写入本地 Chroma。
- 新增 RAG 入库历史，记录模型、集合、分块数和执行状态。

## 验收关注
- 产品经理：确认 RAG 页面能完成“选模型、填文档、入 Chroma、看历史”的业务闭环。
- 架构师：确认 embedding 模型复用模型管理配置，Chroma 默认使用 `http://localhost:9000`。
- 测试：覆盖分块逻辑、接口鉴权放行、空数据和失败提示；Redis 相关不作为阻塞项。

## 下一步
- 补充文件上传、知识库检索问答、批量导入和企业级权限隔离。
